Always include these procedures to synchronize the assets on both routes (`/` and `/app/`) and update the commit according to the context:

In the VS Code PowerShell terminal:



PowerShell
    .\deploy.ps1 "feat: update the commit text here"



Provide the response in Brazilian Portuguese
